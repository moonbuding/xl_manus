{
  "name": "manusxl-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "ManusXL My Computer desktop bridge client.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-renderer.mjs",
    "dev": "npm run build && electron dist/main.js",
    "start": "electron dist/main.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "latest",
    "electron": "latest",
    "typescript": "latest"
  }
}
